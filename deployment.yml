apiVersion: apps/v1
kind: Deployment
metadata:
  name: java
  labels:
    app: java
spec:
  replicas: 2
  selector:
    matchLabels:
      app: java
  template:
    metadata:
      labels:
        app: java
    spec:
      imagePullSecrets:
        - name: ecr-creds
      containers:
        - name: java
          image: diaaqassem1/app-java:v24
          ports:
            - containerPort: 8090
